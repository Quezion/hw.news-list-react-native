(ns example.db)

;; initial state of app-db
(defonce app-db {:articles [{:title "Foo"}
                            {:title "Bar"}
                            {:title "Baz"}
                            {:title "Zot"}]})
